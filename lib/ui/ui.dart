import 'package:flutter/material.dart';

final ButtonStyle myButtonGreenStyle = ElevatedButton.styleFrom(
primary: Colors.green,
minimumSize: const Size.fromHeight(50),
);

final ButtonStyle myButtonBlueStyle = ElevatedButton.styleFrom(
  primary: Colors.blue,
  minimumSize: const Size.fromHeight(50),
);